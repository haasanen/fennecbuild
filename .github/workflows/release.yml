name: Release

# Builds Fennec F-Droid from the fennecbuild recipe (all of Firefox from
# source), signs the unsigned APK and publishes a GitHub release, then
# dispatches the custom F-Droid repo to pick it up.
#
# Free & unlimited on this public repo (standard ubuntu-latest runners).
# Disk checkpoints (df) are logged throughout; a background df logger
# records the whole run so any ENOSPC failure yields a complete disk map.

on:
  # Every push to master starts a full build. (The repo PAT can push but 403s on
  # workflow_dispatch — no admin scope — so the push trigger is the reliable trigger.)
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      version_name:
        description: "versionName"
        required: false
        default: "155.0.0"
      version_code:
        description: "versionCode (scheme XYZAR; R = revision)"
        required: false
        default: "1550020"

env:
  ANDROID_HOME: /usr/lib/android-sdk
  # The runner image bakes ANDROID_SDK_ROOT=/usr/local/lib/android/sdk into its
  # env. gmscore's AGP (Gradle 8.13) refuses to run when the two point at
  # different SDK roots. Pin both to the same directory (F-Droid uses one root).
  ANDROID_SDK_ROOT: /usr/lib/android-sdk
  NDK_VERSION: "29.0.14206865"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    # ci_heartbeat.sh pushes its state file (and, on failure, the build-log
    # tail) to the `ci-logs` branch using the job's GITHUB_TOKEN. Without this
    # the token is read-only by default and every push is a silent 403 —
    # which is why the branch never appeared after the 2026-09-21 runs.
    permissions:
      contents: write
    # One build at a time: parallel runs save to the same cache keys and clobber
    # each other (2026-09-20: two overlapping runs, both cache saves looked OK,
    # the next run's restores still missed). Extra pushes queue instead.
    concurrency:
      group: fennec-build
      cancel-in-progress: false
    steps:
      - name: Checkout fennecbuild
        uses: actions/checkout@v4

      - name: Disk checkpoint 0
        run: |
          echo "=== df (start) ==="; df -h / /home; free -h; nproc

      - name: Install build dependencies (mirrors fdroiddata sudo block)
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake make m4 pkg-config python-is-python3 \
            python3-venv tcl gyp ninja-build bzip2 libffi-dev libsqlite3-dev \
            binutils-dev clang lld libssl-dev zlib1g-dev nodejs terser rustup
          df -h /

      - name: Java 17
        run: |
          ls /usr/lib/jvm/
          export JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64
          echo "JAVA_HOME=$JAVA_HOME" >> "$GITHUB_ENV"
          echo "$JAVA_HOME/bin" >> "$GITHUB_PATH"
          java -version

      - name: Restore tools caches (gradle dists, rust, cargo)
        uses: actions/cache/restore@v4
        with:
          path: |
            /opt/gradle-versions
            ~/.cargo
            ~/.rustup
          # static keys: every input is pinned to an exact commit/version, so
          # the caches stay valid until a pin changes (bump the key suffix then)
          key: fennec-tools-v1

      - name: Gradle (version-aware shim, mirrors F-Droid's per-project wrapper)
        run: |
          # F-Droid does not pin one Gradle globally: each project's build runs on
          # its own gradle-wrapper.properties version (gmscore 8.13, glean/app-services
          # 8.14.3, geckoview/fenix 9.7.0). A single 9.7.0 breaks gmscore (its AGP
          # uses InternalProblems, removed in Gradle 9.6.0). Replicate F-Droid:
          # a shim that reads the current project's wrapper and uses that version.
          install -m 0755 gradle-wrapper.sh /usr/local/bin/gradle
          echo "which gradle -> $(command -v gradle)"
          cat /usr/local/bin/gradle | head -5

      - name: Rust 1.95.0
        run: |
          rustup default 1.95.0
          rustc --version

      - name: Cache Android SDK
        id: sdk-cache
        uses: actions/cache/restore@v4
        with:
          path: /usr/lib/android-sdk
          key: fennec-sdk-r29-37.0-37.1-v1

      - name: Android SDK components (NDK r29, platform, build-tools)
        run: |
          set -e
          # Locate an existing sdkmanager; the runner's cmdline-tools layout varies
          SM=""
          for c in "$ANDROID_HOME"/cmdline-tools/*/bin/sdkmanager "$ANDROID_HOME"/cmdline-tools/bin/sdkmanager; do
            [ -x "$c" ] && SM="$c" && break
          done
          if [ -z "$SM" ]; then
            echo "cmdline-tools not found; installing (same component + checksum fennecbuild build.sh uses)"
            curl --silent -O https://dl.google.com/android/repository/commandlinetools-linux-15641748_latest.zip
            echo 'a66d5ef0238fc0162e9c1446602ce0dd41702d4dd7a94d2ce42d12b7f80baf7e  commandlinetools-linux-15641748_latest.zip' | sha256sum -c -
            unzip -q commandlinetools-linux-15641748_latest.zip -d /tmp/clt
            sudo mkdir -p "$ANDROID_HOME/cmdline-tools/21.0"
            sudo cp -r /tmp/clt/cmdline-tools/. "$ANDROID_HOME/cmdline-tools/21.0/"
            SM="$ANDROID_HOME/cmdline-tools/21.0/bin/sdkmanager"
          fi
          echo "sdkmanager: $SM"
          # The runner's /usr/lib/android-sdk is root-owned: sdkmanager (running as
          # 'runner') cannot persist license acceptance or install properties there
          # ("Failed to read or create install properties file") and re-prompts mid-install.
          # Make the SDK root writable by the current user first.
          sudo chown -R "$USER" "$ANDROID_HOME"
          # put sdkmanager on PATH (build.sh calls bare `sdkmanager`)
          echo "$(cd "$(dirname "$SM")" && pwd)" >> "$GITHUB_PATH"
          # Accept licenses (non-interactive now that the dir is writable)
          yes | "$SM" --licenses
          # Install the components (no prompt expected; keep 'yes' as belt-and-braces)
          yes | "$SM" "ndk;29.0.14206865" "platforms;android-35" "platforms;android-36" "platforms;android-36.1" "platforms;android-37.0" "platforms;android-37.1" "build-tools;37.0.0" "platform-tools"
          # hard check: fail loudly if anything is actually missing
          for d in platforms/android-37.1 platforms/android-35 platforms/android-36 build-tools/37.0.0 ndk/29.0.14206865 platform-tools; do
            [ -d "$ANDROID_HOME/$d" ] || { echo "MISSING $ANDROID_HOME/$d"; ls -la "$ANDROID_HOME"; exit 1; }
          done
          echo "installed platforms:"; ls "$ANDROID_HOME/platforms"
          echo "ANDROID_NDK=$ANDROID_HOME/ndk/29.0.14206865" >> "$GITHUB_ENV"
          echo "ANDROID_SDK=$ANDROID_HOME" >> "$GITHUB_ENV"
          df -h /

      - name: Clone srclibs (pinned, shallow; 3x retry — shared-runner network flakes)
        run: |
          set -e
          SIB=..
          mkdir -p "$SIB/srclib"
          # Run #33 lost 3 of 5 attempts to transient clone failures (exit 128);
          # retry with backoff instead of burning the whole attempt budget.
          gclone() { # <url> <ref> <dest>
            local i
            for i in 1 2 3; do
              if git clone --depth 1 --branch "$2" "$1" "$3"; then return 0; fi
              echo "clone of $3 failed (attempt $i/3), retrying in $((i*30))s"
              rm -rf "$3"
              sleep $((i*30))
            done
            echo "FATAL: clone of $3 failed 3 times"
            return 1
          }
          echo "=== MozFennec (mozilla-central @ FIREFOX-ANDROID_155_0_RELEASE)"
          gclone https://github.com/mozilla-firefox/firefox FIREFOX-ANDROID_155_0_RELEASE "$SIB/srclib/MozFennec"
          df -h /
          echo "=== MozAppServices @ v155.0 (+submodules)"
          gclone https://github.com/mozilla/application-services v155.0 "$SIB/srclib/MozAppServices"
          (cd "$SIB/srclib/MozAppServices" && git submodule update --init --depth 1)
          df -h /
          echo "=== Glean (v68.0.1 / v68.0.0)"
          gclone https://github.com/mozilla/glean v68.0.1 "$SIB/srclib/MozGlean"
          gclone https://github.com/mozilla/glean v68.0.0 "$SIB/srclib/MozGleanAS"
          echo "=== UnifiedPushAC @ 1.0.6"
          gclone https://gitlab.com/ironfox-oss/unifiedpush-ac 1.0.6 "$SIB/srclib/UnifiedPushAC"
          echo "=== gmscore @ v0.3.16.252432"
          gclone https://github.com/microg/GmsCore v0.3.16.252432 "$SIB/srclib/gmscore"
          echo "=== wasi-sdk @ wasi-sdk-20 (+submodules)"
          gclone https://github.com/WebAssembly/wasi-sdk wasi-sdk-20 "$SIB/srclib/wasi-sdk"
          (cd "$SIB/srclib/wasi-sdk" && git submodule update --init --depth 1)
          df -h /
          echo "=== llvm @ 3b5e7c83 (llvm-project commit)"
          git init "$SIB/srclib/llvm"
          (cd "$SIB/srclib/llvm" && \
            git remote add origin https://github.com/llvm/llvm-project && \
            git fetch --depth 1 origin 3b5e7c83a6e226d5bd7ed2e9b67449b64812074c && \
            git checkout -q FETCH_HEAD)
          df -h /
          echo "=== llvm_android @ e727bfb0 (full clone: googlesource SHA checkout)"
          git clone https://android.googlesource.com/toolchain/llvm_android "$SIB/srclib/llvm_android"
          (cd "$SIB/srclib/llvm_android" && git checkout -q e727bfb014bd436f581a66a450c939a6983a1fc3)
          echo "=== toolchain-utils @ dd1ee45a (full clone: googlesource SHA checkout)"
          git clone https://android.googlesource.com/platform/external/toolchain-utils "$SIB/srclib/toolchain-utils"
          (cd "$SIB/srclib/toolchain-utils" && git checkout -q dd1ee45a84cb07337f9d5d0a6769d9b865c6e620)
          df -h /
          echo "=== srclib sizes ==="
          du -sh "$SIB"/srclib/*
          # Absolute path to the srclib dir, for the cache steps below.
          # actions/cache rejects '..' in `path` (and resolves relative paths
          # against the workspace, which is why '../srclib/...' failed), so we
          # hand it an absolute path instead.
          echo "SRCLIB_DIR=$(cd "$SIB/srclib" && pwd)" >> "$GITHUB_ENV"

      - name: prebuild.sh (patches + configure)
        run: |
          chmod +x prebuild.sh build.sh
          echo "gradle used by mozconfig: $(command -v gradle)"
          # inputs.* are empty on push-triggered runs (defaults apply to
          # workflow_dispatch only) — fall back to the release version.
          ./prebuild.sh "${{ inputs.version_name || '155.0.0' }}" "${{ inputs.version_code || '1550020' }}"
          df -h /

      - name: Pin source mtimes (deterministic snapshot for incremental restores)
        run: |
          # Fresh-cloned sources carry mtime=now and the patch step just wrote
          # new files, while a restored obj/ keeps its ORIGINAL build mtimes
          # (actions/cache preserves them). With sources newer than the
          # restored artifacts, cargo/ninja/autoconf treat EVERYTHING as dirty:
          # runs #30-33 showed 874 'Compiling' / 0 'Fresh' crates per segment
          # and a 20s re-configure (config.status stale) — the checkpoint
          # chain never advanced past the first ~377 rust crates. Pin every
          # source file to a fixed timestamp OLDER than any real build, so a
          # restored obj/ is always strictly newer than its inputs and
          # resumes incrementally. Must run AFTER prebuild (patches modify the
          # source tree) and BEFORE the phase-3 restores below.
          SNAP=$(date -d '2026-09-01 00:00:00 UTC' +%s)
          for d in "$SRCLIB_DIR/MozFennec" "$SRCLIB_DIR/MozAppServices"; do
            [ -d "$d" ] || continue
            echo "pinning mtimes under $d -> @${SNAP}"
            # Whole tree, dirs included: a restored obj/target lands here AFTER
            # this step and its artifacts keep their original (newer) build
            # mtimes, so nothing pinned is ever newer than a real artifact.
            find "$d" -depth -exec touch -h -d "@${SNAP}" {} + 2>/dev/null || true
          done
          # Sanity: a pinned source must be strictly older than build time.
          NEW=$(date +%s)
          [ "$SNAP" -lt "$NEW" ] && echo "SNAP=${SNAP} < now=${NEW}: OK"

      - name: Restore LLVM toolchain (if cached from a previous run)
        id: llvm-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/llvm/out
          key: fennec-llvm-out-3b5e7c83-v1

      - name: Restore WASI toolchain (if cached from a previous run)
        id: wasi-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/wasi-sdk/build/install/wasi
          key: fennec-wasi-out-v1

      - name: Phase 1/3 — LLVM toolchain (or restore from cache)
        run: |
          ( while true; do echo "=== $(date -u +%H:%M:%S) ==="; df -h / | tail -1; free -h | head -2; sleep 240; done ) \
            > "$GITHUB_WORKSPACE/df_history.log" 2>&1 &
          echo $! > "$GITHUB_WORKSPACE/df_logger.pid"
          bash ci_heartbeat.sh "$GITHUB_WORKSPACE" "$GITHUB_WORKSPACE/build_llvm.log" &
          echo $! > "$GITHUB_WORKSPACE/heartbeat.pid"
          set -o pipefail
          ./build-llvm.sh 2>&1 | tee "$GITHUB_WORKSPACE/build_llvm.log"
          rc=${PIPESTATUS[0]}
          kill "$(cat "$GITHUB_WORKSPACE/df_logger.pid")" 2>/dev/null || true
          kill "$(cat "$GITHUB_WORKSPACE/heartbeat.pid")" 2>/dev/null || true
          echo "=== df (after LLVM) ==="; df -h /
          exit "$rc"

      - name: Save LLVM toolchain cache (right after the LLVM phase completes)
        run: |
          OUT=../srclib/llvm/out
          if [ ! -e "$OUT/CI_DONE" ]; then echo "no CI_DONE marker — not saving"; exit 0; fi
          if [ ! -d "$OUT" ]; then echo "LLVM out dir missing — not saving"; exit 0; fi
          b=$(du -sb "$OUT" 2>/dev/null | awk '{print $1}')
          if [ -z "$b" ]; then
            b=$(( $(du -sk "$OUT" 2>/dev/null | awk '{print $1}' || echo 0) * 1024 ))
          fi
          gb=$(awk -v b="${b:-0}" 'BEGIN{printf "%.1f", b/1073741824}')
          if awk -v g="$gb" 'BEGIN{exit !(g < 9.5)}'; then
            echo "LLVM out = ${gb} GB — saving cache"
            echo "LLVM_OUT=1" >> "$GITHUB_ENV"
          else
            echo "LLVM out = ${gb} GB — skipping save (>= 9.5 GB cache limit)"
          fi
          # A cache-save hiccup must NEVER sink the build (run 35498864447 failed
          # here at ~60 min, after a successful LLVM compile). Exit 0 unconditionally.
          exit 0
      - name: Cache LLVM out
        if: env.LLVM_OUT == '1'
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/llvm/out
          key: fennec-llvm-out-3b5e7c83-v1

      - name: Phase 2/3 — WASI SDK (or restore from cache)
        run: |
          ( while true; do echo "=== $(date -u +%H:%M:%S) ==="; df -h / | tail -1; free -h | head -2; sleep 240; done ) \
            >> "$GITHUB_WORKSPACE/df_history.log" 2>&1 &
          echo $! > "$GITHUB_WORKSPACE/df_logger.pid"
          bash ci_heartbeat.sh "$GITHUB_WORKSPACE" "$GITHUB_WORKSPACE/build_wasi.log" &
          echo $! > "$GITHUB_WORKSPACE/heartbeat.pid"
          set -o pipefail
          ./build-wasi.sh 2>&1 | tee "$GITHUB_WORKSPACE/build_wasi.log"
          rc=${PIPESTATUS[0]}
          kill "$(cat "$GITHUB_WORKSPACE/df_logger.pid")" 2>/dev/null || true
          kill "$(cat "$GITHUB_WORKSPACE/heartbeat.pid")" 2>/dev/null || true
          echo "=== df (after WASI) ==="; df -h /
          exit "$rc"

      - name: Save WASI toolchain cache (right after the WASI phase completes)
        run: |
          OUT=../srclib/wasi-sdk/build/install/wasi
          if [ ! -e "$OUT/CI_DONE" ]; then echo "no CI_DONE marker — not saving"; exit 0; fi
          if [ ! -d "$OUT" ]; then echo "WASI install dir missing — not saving"; exit 0; fi
          b=$(du -sb "$OUT" 2>/dev/null | awk '{print $1}')
          if [ -z "$b" ]; then
            b=$(( $(du -sk "$OUT" 2>/dev/null | awk '{print $1}' || echo 0) * 1024 ))
          fi
          gb=$(awk -v b="${b:-0}" 'BEGIN{printf "%.1f", b/1073741824}')
          if awk -v g="$gb" 'BEGIN{exit !(g < 9.5)}'; then
            echo "WASI install = ${gb} GB — saving cache"
            echo "WASI_OUT=1" >> "$GITHUB_ENV"
          else
            echo "WASI install = ${gb} GB — skipping save (>= 9.5 GB cache limit)"
          fi
          exit 0
      - name: Cache WASI out
        if: env.WASI_OUT == '1'
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/wasi-sdk/build/install/wasi
          key: fennec-wasi-out-v1

      # ---- Phase 3: resumable stages --------------------------------------
      # Phase 3 used to be ONE build-rest.sh step; when the shared
      # ubuntu-latest runner was recycled mid-phase ("The runner has received a
      # shutdown signal", exit 143) everything after the last cache was lost.
      # It is now five sequential stages (same order as build-rest.sh, via
      # STAGES= filtering), and EVERY completed stage uploads a checkpoint
      # cache before the next stage starts. Keys carry run_id + run_attempt +
      # stage because actions/cache refuses to overwrite an existing key on the
      # same branch; restore-keys below picks up the most recent checkpoint of
      # any previous run/attempt, so a re-run resumes from the last completed
      # stage instead of restarting Phase 3 from zero (already-done stages
      # re-execute as fast up-to-date no-ops against the restored
      # obj/target/m2/gradle dirs).
      - name: Restore phase-3 caches (mavenLocal, gradle home, build dirs)
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gradle
          # per-run seed key (never saved) + prefix restore of the newest
          # checkpoint saved by any earlier run or attempt
          key: fennec-p3-deps-v1-${{ github.run_id }}-${{ github.run_attempt }}-seed
          restore-keys: |
            fennec-p3-deps-v1-
            fennec-p3-deps-v1

      - name: Restore phase-3 build dirs (incremental compile)
        uses: actions/cache/restore@v4
        with:
          path: |
            ${{ env.SRCLIB_DIR }}/MozFennec/obj
            ${{ env.SRCLIB_DIR }}/MozAppServices/target
          # Separate key (independent 10 GB budget): a build-dir save that
          # outgrows its limit must not poison the dependency cache. On a hit,
          # Phase 3 resumes incrementally (Gradle/Rust reuse valid artifacts).
          key: fennec-p3-build-v1-${{ github.run_id }}-${{ github.run_attempt }}-seed
          restore-keys: |
            fennec-p3-build-v1-
            fennec-p3-build-v1

      - name: P3b1 restore latest mach-build segment
        # Newest time-boxed `mach build` checkpoint of ANY previous run or
        # attempt (actions/cache returns the most-recently-created cache
        # matching a restore-key prefix — documented restore semantics).
        # Saved by the segment loop below via midstage_cache_saver.sh (same
        # wire format as actions/cache/save, so this restores it unchanged).
        # MUST list EXACTLY the same path string as the segment saves: the
        # cache 'version' hash is sha256(paths|method|salt) of these literals.
        # Runs AFTER the generic build-dirs restore and lays on top of it.
        # continue-on-error: a corrupt/oversize cached checkpoint must never
        # hard-kill the job pre-P3a — a bad restore just means the segment
        # loop starts fresh from the (older) build-dirs restore underneath.
        continue-on-error: true
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seed
          restore-keys: |
            fennec-p3b1-seg-v1-

      - name: Phase 3 heartbeat (one logger for all p3 stages)
        run: |
          ( while true; do echo "=== $(date -u +%H:%M:%S) ==="; df -h / | tail -1; free -h | head -2; sleep 240; done ) \
            >> "$GITHUB_WORKSPACE/df_history.log" 2>&1 &
          echo $! > "$GITHUB_WORKSPACE/df_logger.pid"
          bash ci_heartbeat.sh "$GITHUB_WORKSPACE" "$GITHUB_WORKSPACE/build_full.log" &
          echo $! > "$GITHUB_WORKSPACE/heartbeat.pid"

      - name: Phase 3a — microG libs + Glean (publishToMavenLocal)
        id: p3a
        run: bash run_p3_stage.sh "gmscore glean glean_as" p3a

      - name: P3a checkpoint — deps
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gradle
          key: fennec-p3-deps-v1-${{ github.run_id }}-${{ github.run_attempt }}-a

      # ---- P3b split: build / package / geckoview --------------------------
      # The old single P3b step ran `mach build && mach package &&
      # package-multi-locale + :geckoview:publish` (hours). Shared runners
      # get reclaimed ~10-30 min in (run #25: SIGTERM at mach clock 11:02),
      # and a reclaimed step saved NOTHING — the next attempt recompiled
      # from zero. P3b is now three boundary-checkpointed sub-stages
      # (build-rest.sh STAGES gecko_build / gecko_package / gecko_gv), and
      # gecko_build runs as a STATIC expansion of 24 time-boxed segment
      # steps (run_p3_segment.sh, 10-min budget each) followed by a step
      # boundary actions/cache/save@v4 checkpoint of obj/. Once mach build
      # completes (rc=0 -> done=true) every remaining segment+save step
      # short-circuits via its `if:` and the build continues at P3b2.
      #
      # WHY step-boundary saves and not the in-step saver (run #29 lesson):
      # 2026 hosted runners ("Hosted Compute Agent", runner 2.337.0) expose
      # NO cache-service env to bash steps — ACTIONS_CACHE_URL,
      # ACTIONS_RESULTS_URL, ACTIONS_RUNTIME_TOKEN are all UNSET (probe run
      # 35944549562, 2026-09-24) — so midstage_cache_saver.sh SKIPs on its
      # env preflight. actions/cache@v4 still works at step boundaries
      # (deps caches land), so checkpoints must live where v4 can run.
      # Losing a segment to a mid-step reclaim costs at most its 10 min.
      #
      # NOTE: the `path:` in every save step MUST stay byte-identical to the
      # `P3b1 restore latest mach-build segment` restore step above — the
      # cache 'version' hash is computed over the literal path string.
      - name: "P3b1 seg 1 — mach build (10-min budget)"
        id: seg1
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s1" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 1"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 1 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 1 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg1.conclusion == 'success' && steps.seg1.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg1

      - name: "P3b1 seg 2 — mach build (10-min budget)"
        id: seg2
        if: ${{ steps.seg1.conclusion == 'success' && steps.seg1.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s2" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 2"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 2 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 2 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg2.conclusion == 'success' && steps.seg2.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg2

      - name: "P3b1 seg 3 — mach build (10-min budget)"
        id: seg3
        if: ${{ steps.seg2.conclusion == 'success' && steps.seg2.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s3" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 3"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 3 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 3 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg3.conclusion == 'success' && steps.seg3.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg3

      - name: "P3b1 seg 4 — mach build (10-min budget)"
        id: seg4
        if: ${{ steps.seg3.conclusion == 'success' && steps.seg3.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s4" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 4"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 4 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 4 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg4.conclusion == 'success' && steps.seg4.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg4

      - name: "P3b1 seg 5 — mach build (10-min budget)"
        id: seg5
        if: ${{ steps.seg4.conclusion == 'success' && steps.seg4.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s5" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 5"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 5 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 5 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg5.conclusion == 'success' && steps.seg5.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg5

      - name: "P3b1 seg 6 — mach build (10-min budget)"
        id: seg6
        if: ${{ steps.seg5.conclusion == 'success' && steps.seg5.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s6" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 6"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 6 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 6 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg6.conclusion == 'success' && steps.seg6.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg6

      - name: "P3b1 seg 7 — mach build (10-min budget)"
        id: seg7
        if: ${{ steps.seg6.conclusion == 'success' && steps.seg6.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s7" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 7"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 7 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 7 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg7.conclusion == 'success' && steps.seg7.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg7

      - name: "P3b1 seg 8 — mach build (10-min budget)"
        id: seg8
        if: ${{ steps.seg7.conclusion == 'success' && steps.seg7.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s8" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 8"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 8 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 8 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg8.conclusion == 'success' && steps.seg8.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg8

      - name: "P3b1 seg 9 — mach build (10-min budget)"
        id: seg9
        if: ${{ steps.seg8.conclusion == 'success' && steps.seg8.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s9" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 9"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 9 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 9 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg9.conclusion == 'success' && steps.seg9.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg9

      - name: "P3b1 seg 10 — mach build (10-min budget)"
        id: seg10
        if: ${{ steps.seg9.conclusion == 'success' && steps.seg9.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s10" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 10"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 10 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 10 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg10.conclusion == 'success' && steps.seg10.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg10

      - name: "P3b1 seg 11 — mach build (10-min budget)"
        id: seg11
        if: ${{ steps.seg10.conclusion == 'success' && steps.seg10.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s11" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 11"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 11 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 11 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg11.conclusion == 'success' && steps.seg11.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg11

      - name: "P3b1 seg 12 — mach build (10-min budget)"
        id: seg12
        if: ${{ steps.seg11.conclusion == 'success' && steps.seg11.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s12" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 12"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 12 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 12 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg12.conclusion == 'success' && steps.seg12.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg12

      - name: "P3b1 seg 13 — mach build (10-min budget)"
        id: seg13
        if: ${{ steps.seg12.conclusion == 'success' && steps.seg12.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s13" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 13"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 13 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 13 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg13.conclusion == 'success' && steps.seg13.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg13

      - name: "P3b1 seg 14 — mach build (10-min budget)"
        id: seg14
        if: ${{ steps.seg13.conclusion == 'success' && steps.seg13.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s14" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 14"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 14 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 14 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg14.conclusion == 'success' && steps.seg14.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg14

      - name: "P3b1 seg 15 — mach build (10-min budget)"
        id: seg15
        if: ${{ steps.seg14.conclusion == 'success' && steps.seg14.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s15" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 15"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 15 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 15 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg15.conclusion == 'success' && steps.seg15.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg15

      - name: "P3b1 seg 16 — mach build (10-min budget)"
        id: seg16
        if: ${{ steps.seg15.conclusion == 'success' && steps.seg15.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s16" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 16"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 16 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 16 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg16.conclusion == 'success' && steps.seg16.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg16

      - name: "P3b1 seg 17 — mach build (10-min budget)"
        id: seg17
        if: ${{ steps.seg16.conclusion == 'success' && steps.seg16.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s17" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 17"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 17 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 17 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg17.conclusion == 'success' && steps.seg17.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg17

      - name: "P3b1 seg 18 — mach build (10-min budget)"
        id: seg18
        if: ${{ steps.seg17.conclusion == 'success' && steps.seg17.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s18" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 18"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 18 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 18 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg18.conclusion == 'success' && steps.seg18.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg18

      - name: "P3b1 seg 19 — mach build (10-min budget)"
        id: seg19
        if: ${{ steps.seg18.conclusion == 'success' && steps.seg18.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s19" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 19"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 19 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 19 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg19.conclusion == 'success' && steps.seg19.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg19

      - name: "P3b1 seg 20 — mach build (10-min budget)"
        id: seg20
        if: ${{ steps.seg19.conclusion == 'success' && steps.seg19.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s20" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 20"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 20 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 20 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg20.conclusion == 'success' && steps.seg20.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg20

      - name: "P3b1 seg 21 — mach build (10-min budget)"
        id: seg21
        if: ${{ steps.seg20.conclusion == 'success' && steps.seg20.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s21" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 21"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 21 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 21 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg21.conclusion == 'success' && steps.seg21.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg21

      - name: "P3b1 seg 22 — mach build (10-min budget)"
        id: seg22
        if: ${{ steps.seg21.conclusion == 'success' && steps.seg21.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s22" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 22"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 22 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 22 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg22.conclusion == 'success' && steps.seg22.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg22

      - name: "P3b1 seg 23 — mach build (10-min budget)"
        id: seg23
        if: ${{ steps.seg22.conclusion == 'success' && steps.seg22.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s23" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 23"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 23 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 23 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg23.conclusion == 'success' && steps.seg23.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg23

      - name: "P3b1 seg 24 — mach build (10-min budget)"
        id: seg24
        if: ${{ steps.seg23.conclusion == 'success' && steps.seg23.outputs.done == 'false' }}
        env:
          SEGMENT_SECS: "1200"   # 20 min: a 10-min segment spent ~3 min on
          # per-segment gradle/JNI restart + ~1.5 min checkpoint upload (~45%
          # overhead). 20 min amortizes that; worst-case reclaim loss is still
          # bounded to one segment (save runs at the boundary).
        run: |
          set -u
          rc=0
          bash run_p3_segment.sh "gecko_build" "p3b1-s24" "${SEGMENT_SECS}" || rc=$?
          if [ "$rc" = "0" ]; then
            echo "P3b1: mach build complete in segment 24"
            echo "done=true" >> "$GITHUB_OUTPUT"
          elif [ "$rc" = "124" ]; then
            echo "P3b1: segment 24 deadline hit — obj checkpoint follows"
            echo "done=false" >> "$GITHUB_OUTPUT"
          else
            echo "P3b1: real build failure rc=$rc"
            exit "$rc"
          fi

      - name: "P3b1 seg 24 — save obj checkpoint (v4)"
        # Step-boundary save: 2026 runners expose NO cache-service env to
        # bash steps (probe 2026-09-24), so in-step saving is impossible;
        # actions/cache@v4 at a step boundary is the only working channel.
        # Path MUST match the restore step above exactly (version-hash parity).
        # continue-on-error: a failed/oversize save must not kill a healthy
        # build — the next segment simply re-does that 10 min.
        if: ${{ steps.seg24.conclusion == 'success' && steps.seg24.outputs.done == 'false' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SRCLIB_DIR }}/MozFennec/obj
          key: fennec-p3b1-seg-v1-${{ github.run_id }}-${{ github.run_attempt }}-seg24

      - name: "P3b1 cap check (mach build incomplete after 24 segments)"
        if: ${{ !(steps.seg1.outputs.done == 'true' || steps.seg2.outputs.done == 'true' || steps.seg3.outputs.done == 'true' || steps.seg4.outputs.done == 'true' || steps.seg5.outputs.done == 'true' || steps.seg6.outputs.done == 'true' || steps.seg7.outputs.done == 'true' || steps.seg8.outputs.done == 'true' || steps.seg9.outputs.done == 'true' || steps.seg10.outputs.done == 'true' || steps.seg11.outputs.done == 'true' || steps.seg12.outputs.done == 'true' || steps.seg13.outputs.done == 'true' || steps.seg14.outputs.done == 'true' || steps.seg15.outputs.done == 'true' || steps.seg16.outputs.done == 'true' || steps.seg17.outputs.done == 'true' || steps.seg18.outputs.done == 'true' || steps.seg19.outputs.done == 'true' || steps.seg20.outputs.done == 'true' || steps.seg21.outputs.done == 'true' || steps.seg22.outputs.done == 'true' || steps.seg23.outputs.done == 'true' || steps.seg24.outputs.done == 'true') }}
        run: |
          echo "P3b1: 24 segments (10 min each) exhausted without completion"
          exit 1


      - name: P3b1 checkpoint — build dirs (mach build done)
        if: ${{ steps.seg1.outputs.done == 'true' || steps.seg2.outputs.done == 'true' || steps.seg3.outputs.done == 'true' || steps.seg4.outputs.done == 'true' || steps.seg5.outputs.done == 'true' || steps.seg6.outputs.done == 'true' || steps.seg7.outputs.done == 'true' || steps.seg8.outputs.done == 'true' || steps.seg9.outputs.done == 'true' || steps.seg10.outputs.done == 'true' || steps.seg11.outputs.done == 'true' || steps.seg12.outputs.done == 'true' || steps.seg13.outputs.done == 'true' || steps.seg14.outputs.done == 'true' || steps.seg15.outputs.done == 'true' || steps.seg16.outputs.done == 'true' || steps.seg17.outputs.done == 'true' || steps.seg18.outputs.done == 'true' || steps.seg19.outputs.done == 'true' || steps.seg20.outputs.done == 'true' || steps.seg21.outputs.done == 'true' || steps.seg22.outputs.done == 'true' || steps.seg23.outputs.done == 'true' || steps.seg24.outputs.done == 'true' }}
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ${{ env.SRCLIB_DIR }}/MozFennec/obj
            ${{ env.SRCLIB_DIR }}/MozAppServices/target
          key: fennec-p3-build-v1-${{ github.run_id }}-${{ github.run_attempt }}-b1

      - name: Phase 3b2 — Gecko package + package-multi-locale
        id: p3b2
        run: bash run_p3_stage.sh "gecko_package" p3b2

      - name: Phase 3b3 — GeckoView publishToMavenLocal
        id: p3b3
        run: bash run_p3_stage.sh "gecko_gv" p3b3

      - name: P3b checkpoint guard (build dirs < 9 GB)
        id: ck-b
        continue-on-error: true
        run: bash ci_p3_guard.sh

      - name: P3b checkpoint — deps
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gradle
          key: fennec-p3-deps-v1-${{ github.run_id }}-${{ github.run_attempt }}-b

      - name: P3b checkpoint — build dirs
        if: steps.ck-b.conclusion == 'success'
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ${{ env.SRCLIB_DIR }}/MozFennec/obj
            ${{ env.SRCLIB_DIR }}/MozAppServices/target
          key: fennec-p3-build-v1-${{ github.run_id }}-${{ github.run_attempt }}-b

      - name: Phase 3c — AppServices (concept-fetch shim, NSS, nimbus-fml)
        id: p3c
        run: bash run_p3_stage.sh "ac_fetch150 appservices" p3c

      - name: P3c checkpoint guard (build dirs < 9 GB)
        id: ck-c
        continue-on-error: true
        run: bash ci_p3_guard.sh

      - name: P3c checkpoint — deps
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gradle
          key: fennec-p3-deps-v1-${{ github.run_id }}-${{ github.run_attempt }}-c

      - name: P3c checkpoint — build dirs
        if: steps.ck-c.conclusion == 'success'
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ${{ env.SRCLIB_DIR }}/MozFennec/obj
            ${{ env.SRCLIB_DIR }}/MozAppServices/target
          key: fennec-p3-build-v1-${{ github.run_id }}-${{ github.run_attempt }}-c

      - name: Phase 3d — UnifiedPush + Android Components
        id: p3d
        run: bash run_p3_stage.sh "upush ac" p3d

      - name: P3d checkpoint — deps
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gradle
          key: fennec-p3-deps-v1-${{ github.run_id }}-${{ github.run_attempt }}-d

      - name: Phase 3e — Fenix assembleRelease
        id: p3e
        run: bash run_p3_stage.sh "fenix" p3e

      - name: P3e checkpoint guard (build dirs < 9 GB)
        id: ck-e
        continue-on-error: true
        run: bash ci_p3_guard.sh

      - name: P3e checkpoint — deps
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.m2/repository
            ~/.gradle
          key: fennec-p3-deps-v1-${{ github.run_id }}-${{ github.run_attempt }}-e

      - name: P3e checkpoint — build dirs
        if: steps.ck-e.conclusion == 'success'
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ${{ env.SRCLIB_DIR }}/MozFennec/obj
            ${{ env.SRCLIB_DIR }}/MozAppServices/target
          key: fennec-p3-build-v1-${{ github.run_id }}-${{ github.run_attempt }}-e

      - name: Stop Phase 3 heartbeat
        if: always()
        run: |
          kill "$(cat "$GITHUB_WORKSPACE/df_logger.pid")" 2>/dev/null || true
          kill "$(cat "$GITHUB_WORKSPACE/heartbeat.pid")" 2>/dev/null || true
          echo "=== df (end) ==="; df -h /

      - name: Upload build logs (diagnostics)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          if-no-files-found: ignore
          path: |
            df_history.log
            build_llvm.log
            build_wasi.log
            build_full.log
            build_p3a.log
            build_p3b.log
            build_p3c.log
            build_p3d.log
            build_p3e.log
            ci_heartbeat_debug.log
            heartbeat_state.txt
            midstage_saver.log
      - name: Locate unsigned APK
        id: apk
        run: |
          set -e
          APKDIR=../srclib/MozFennec/obj/gradle/build/mobile/android/fenix/app/outputs/apk/release
          ls -la "$APKDIR"
          shopt -s nullglob
          apks=("$APKDIR"/app-*-release-unsigned.apk)
          [ ${#apks[@]} -gt 0 ] || { echo "NO APK FOUND"; tail -80 "$GITHUB_WORKSPACE/build_full.log"; exit 1; }
          # arm64 for the Pixel 8a
          pick=""
          for a in "${apks[@]}"; do case "$a" in *arm64*) pick="$a";; esac; done
          [ -n "$pick" ] || pick="${apks[0]}"
          echo "apk=$pick" >> "$GITHUB_OUTPUT"
          echo "Picked: $pick"; ls -la "$pick"

      - name: Sign APK (permanent release key)
        if: steps.apk.outcome == 'success'
        env:
          FENNEC_KEYSTORE_BASE64: ${{ secrets.FENNEC_KEYSTORE_BASE64 }}
          FENNEC_STORE_PASS: ${{ secrets.FENNEC_STORE_PASS }}
          FENNEC_KEY_PASS: ${{ secrets.FENNEC_KEY_PASS }}
        run: |
          set -e
          BT="$ANDROID_HOME/build-tools/37.0.0"
          [ -x "$BT/apksigner" ] || { echo "apksigner missing"; ls "$BT"; exit 1; }
          echo "$FENNEC_KEYSTORE_BASE64" | base64 -d > fennec-release.jks
          UNSIGNED="${{ steps.apk.outputs.apk }}"
          SIGNED="Fennec-${{ inputs.version_code || '1550020' }}-${GITHUB_SHA::7}.apk"
          cp "$UNSIGNED" unsigned.apk
          "$BT/zipalign" -f 4 unsigned.apk aligned.apk
          "$BT/apksigner" sign \
            --ks fennec-release.jks --ks-key-alias fennecrelease \
            --ks-pass "pass:$FENNEC_STORE_PASS" --key-pass "pass:$FENNEC_KEY_PASS" \
            --out "$SIGNED" aligned.apk
          "$BT/apksigner" verify --print-certs "$SIGNED"
          ls -la "$SIGNED"
          echo "SIGNED_APK=$SIGNED" >> "$GITHUB_ENV"
          echo "artifact=$SIGNED" >> "$GITHUB_OUTPUT"

      - name: Upload signed APK (artifact)
        if: steps.apk.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: fennec-signed
          path: ${{ env.SIGNED_APK }}

      - name: Publish GitHub release
        if: steps.apk.outcome == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          ls -la Fennec-*.apk
          TAG="fennec-${{ inputs.version_name || '155.0.0' }}-${{ inputs.version_code || '1550020' }}"
          NOTES_FILE="$(mktemp)"
          {
            echo "Fennec F-Droid fork — Firefox ${{ inputs.version_name || '155.0.0' }} (versionCode ${{ inputs.version_code || '1550020' }})"
            echo ""
            echo "Built from haasanen/fennecbuild @ ${GITHUB_SHA}"
            echo "Same base as the official Fennec F-Droid build; signed with the fork's permanent release key."
          } > "$NOTES_FILE"
          gh release create "$TAG" "Fennec-"*.apk \
            --title "Fennec ${{ inputs.version_name || '155.0.0' }} (${{ inputs.version_code || '1550020' }})" \
            --notes-file "$NOTES_FILE" || \
          gh release upload "$TAG" "Fennec-"*.apk --clobber

      - name: Notify F-Droid repo (push trigger, fail-open)
        if: steps.apk.outcome == 'success'
        env:
          FDROID_DISPATCH_TOKEN: ${{ secrets.FDROID_DISPATCH_TOKEN }}
        run: |
          [ -z "${FDROID_DISPATCH_TOKEN:-}" ] && { echo "no dispatch token, skipping"; exit 0; }
          curl -sS -o /dev/null -w "dispatch HTTP %{http_code}\n" -X POST \
            -H "Authorization: Bearer $FDROID_DISPATCH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/haasanen/fdroid-repo/actions/workflows/update.yml/dispatches \
            -d '{"ref":"main"}' || true
